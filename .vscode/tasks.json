{
    "tasks": [
        {
            "type": "shell",
            "command": [
                "nix develop -c scripts/build.sh -ad || scripts/build.sh -ad"
            ],
            "problemMatcher": [
                "$rustc"
            ],
            "group": "build",
            "label": "build all"
        }
    ]
}